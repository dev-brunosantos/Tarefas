import { StyleSheet } from "react-native";

export const PagesStyles = StyleSheet.create({
    page: {
        flex: 1,
        padding: 20,
        alignItems: 'center',
        justifyContent: 'center'
    },
    
})